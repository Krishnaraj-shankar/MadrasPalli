<!-- views/index.hbs -->
<div>
    <h1>{{ title }}</h1>
    {{#if author}}
    <p>Written by {{ author }}</p>
    {{/if}}
    <ul>
        {{#each items}}
        <li>{{ this }}</li>
        {{/each}}
    </ul>

    <!-- Include a partial -->
    {{> footer}}
</div>