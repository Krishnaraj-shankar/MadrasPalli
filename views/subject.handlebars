<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />  
    <title>My Awesome App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link rel="stylesheet" type="text/css" href="./subject.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

  </head>
  <body>
    {{> nav}}

    <div class="subject-title">
      <p class="title-m">Grade & Subject</p>
    </div>

    <div class="video-container">

      <div class="video-player">
        video player will be here
      </div>

      <button class="btn">click</button>

      <div class="video-data">

        <p class="curr-video">Current video title</p>

        {{!-- {{#if data}}
            <p>Name: {{data}}</p>
            <script>
                console.log("User is an admin:", JSON.parse({{data}}));
            </script>
        {{else}}
            <p>No user data available.</p>
        {{/if}} --}}
      </div>


    </div>



<script>

let sendDataBtn = document.querySelector('button');


  sendDataBtn.addEventListener('click', function() {
        // Create an object with the data you want to send
        var data = { name: 'John', age: 25 };

        // Create a new XMLHttpRequest object
        var xhr = new XMLHttpRequest();
        
        // Set up the request
        xhr.open('POST', '/subject', true);
        xhr.setRequestHeader('Content-Type', 'application/json');

        // Set up the onload callback function
        xhr.onload = function() {
          if (xhr.status === 200) {
            console.log('Server response:', responseText);
          } else {
            console.error('Error:', xhr.status);
          }
        };

        // Send the request
        xhr.send(jsonData);
      });

</script>


    
  </body>
</html>

